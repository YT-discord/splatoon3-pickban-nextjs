{
  "name": "aplatoon3-pickban-appilication",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "common"
  ],
  "scripts": {
    "build": "npm run build --workspace=client && npm run build --workspace=server"
  }
}
